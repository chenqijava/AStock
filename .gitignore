# ─── 密钥/凭证：禁止提交 ───────────────────────────────
# TG bot token + chat_id（敏感！）
tg_config.json

# PushPlus token（微信推送, 敏感！）
pushplus_config.json

# Server酱 sendkey（微信推送, 敏感！）
serverchan_config.json

# ─── 每日循环状态与日志 ────────────────────────────────
daily_loop_state.json
daily_loop.log

# ─── 实盘台账/成交/信号：个人持仓数据，勿入库 ──────────
panic_positions.json
signals_*.csv
fills_*.csv

# ─── 行情数据目录：体积大且可重新下载，不入库 ──────────
# 不复权日线 / 后复权日线 / 退市股后复权
a_share_daily/
a_share_daily_hfq/
a_share_delisted_hfq/

# ─── Python 缓存 ──────────────────────────────────────
__pycache__/
*.pyc
